document.getElementById('clickme').addEventListener('click', function() {
    alert('click')
    console.log('click')
    // const character = document.querySelector('input[type="radio"][name="fileType"]:checked').value
    // if (character === 'other') {
    //     character = document.getElementById('otherInput').value
    // }
    // const text = document.getElementById("article").innerText
    // const lines = text.split('\n')
    // const csvContent = lines.map(line => line.trim()).filter(line => line).map(line => `${character},${line}`).join('\n')

    // downloadCSV(csvContent, `${document.getElementsByTagName("Title")[0].innerText}.csv`)

});
